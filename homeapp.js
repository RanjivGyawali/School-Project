function scrollToSection(targetId) {
    // Get the target section's top offset
    const targetSection = document.getElementById('targetId');
    const targetOffset = targetSection.offsetTop;

    // Scroll down to the target section
    window.scrollTo({
        top: targetOffset,
        behavior: 'smooth' // Optional: Adds a smooth scrolling effect
    });
}

function texas() {
    // Get the target section's top offset
    const targetSection = document.getElementById('target-section2');
    const targetOffset = targetSection.offsetTop;

    // Scroll down to the target section
    window.scrollTo({
        top: targetOffset,
        behavior: 'smooth' // Optional: Adds a smooth scrolling effect
    });
}
function about() {
    // Get the target section's top offset
    const targetSection = document.getElementById('target-section3');
    const targetOffset = targetSection.offsetTop;

    // Scroll down to the target section
    window.scrollTo({
        top: targetOffset,
        behavior: 'smooth' // Optional: Adds a smooth scrolling effect
    });
}